---
interface Props {
	token: string;
}

const { token } = Astro.props;
---

<!-- Cloudflare Web Analytics -->
<script is:inline data-swup-ignore-script defer src='https://static.cloudflareinsights.com/beacon.min.js' data-cf-beacon={`{"token": "${token}"}`}></script>
